import React from 'react'
import { View } from 'react-native'
import {Colors} from '../../theme'

const Line = (props) => {

    return (
        <View style={props.styles} />
    )
}

export default Line
